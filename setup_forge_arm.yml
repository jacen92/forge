- hosts: test
  vars:
  - FORGE_MODE: "test"
  - DOMAIN_NAME: ""
  - FEATURES: ["vault"]
  - USER_NAME: "pi"
  - USER_MAIL: "{{ USER_NAME}}@{{ DOMAIN_NAME }}"
  - DATACORE: "home/{{ USER_NAME }}/data"
  roles:
    - common
    - infra
    - infra_ci
    - infra_hard
    - infra_media
    - infra_storage
